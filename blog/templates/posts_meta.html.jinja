{% for post in posts %}
  .{{ post.slug }}-img {
  background-image: url('{{post.featured_image}}');
  }

  .{{post.slug}}-gradient {
  background: linear-gradient(to right, {{ post.gradient[0] }} 0%, {{ post.gradient[1] }} 100%);
  }
{% endfor %}
